name: CI

on:
  push:
    branches: [main, foundation]
  pull_request:
    branches: [main, foundation]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.12', '3.13']
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml
      - name: Install
        run: python -m pip install -e ".[dev]"
      - name: Ruff
        run: python -m ruff check src tests tools/verify_distribution.py tools/verify_installed_distribution.py tools/benchmark_validation.py tools/release_check.py tools/capture_ci_diagnostics.py
      - name: Mypy
        run: python -m mypy src/bluewater tools/verify_distribution.py tools/verify_installed_distribution.py
      - name: Tests
        run: python -m pytest --cov=bluewater --cov-branch --cov-report=term-missing --cov-report=xml --junitxml=test-results.xml
      - name: Build distributions
        run: python -m build
      - name: Verify package metadata
        run: python -m twine check dist/*
      - name: Verify distribution contents
        run: python tools/verify_distribution.py dist
      - name: Verify installed distribution
        run: python tools/verify_installed_distribution.py dist
      - name: Machine-readable governance
        run: python tools/capture_ci_diagnostics.py
      - name: Governance
        if: success()
        run: python -m bluewater check --scope all
      - name: Upload distributions
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: distributions-${{ matrix.os }}-py${{ matrix.python-version }}
          path: dist/*
          if-no-files-found: warn
      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: diagnostics-${{ matrix.os }}-py${{ matrix.python-version }}
          path: |
            bluewater-diagnostics.json
            test-results.xml
            coverage.xml
          if-no-files-found: warn
